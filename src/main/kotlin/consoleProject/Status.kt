package consoleProject

enum class Status(val message : String) {

    // these are enum class objects
    PENDING("order is pending"),
    CANCELLED("order cancelled"),
    CONFIRMED("Congratulations Your Order is confirmed ");

}